"""
Configuration Redis pour Phase 3.2 (à ajouter à .env)

Development:
  REDIS_HOST=localhost
  REDIS_PORT=6379
  REDIS_DB=0
  REDIS_PASSWORD=
  CACHE_ENABLED=true
  CACHE_DEBUG=false
  CACHE_TTL_LISTING=3600
  CACHE_TTL_USER=1800
  CACHE_TTL_SEARCH=300

Production:
  REDIS_HOST=redis.prod.example.com
  REDIS_PORT=6379
  REDIS_DB=0
  REDIS_PASSWORD=secure_password_here
  CACHE_ENABLED=true
  CACHE_DEBUG=false
  CACHE_TTL_LISTING=3600
  CACHE_TTL_USER=1800
  CACHE_TTL_SEARCH=300

Docker Compose:
  services:
    redis:
      image: redis:7-alpine
      ports:
        - "6379:6379"
      volumes:
        - redis_data:/data
      command: redis-server --appendonly yes

volumes:
  redis_data:
"""

# .env EXAMPLE - Add to .env:
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
CACHE_ENABLED=true
CACHE_DEBUG=false
CACHE_TTL_LISTING=3600
CACHE_TTL_USER=1800
CACHE_TTL_SEARCH=300
CACHE_TTL_NOTIF=120
